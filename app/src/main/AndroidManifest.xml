<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.projectandroidfinal">

    <application
        android:allowBackup="true"
        android:icon="@drawable/whowantsmillonicon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="false"
        android:theme="@style/Theme.ProjectAndroidFinal"
        >
        <activity android:name=".Difficult_Activity"></activity>
        <activity android:name=".High_Score_Activity" />
        <activity android:name=".Prize_Activity" />
        <activity android:name=".Question_Activity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>